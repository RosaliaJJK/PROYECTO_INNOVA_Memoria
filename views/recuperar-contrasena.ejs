<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Restablecer Contraseña - INNOVA SIGET</title>
    <link rel="stylesheet" href="/Style.css" />
    
    <style>
        /* Estilos específicos para centrar el panel de recuperación */
        body {
            background: #f6f5f7;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            font-family: 'Montserrat', sans-serif;
            height: 100vh;
            margin: -20px 0 50px;
        }

        .container-reset {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 14px 28px rgba(0,0,0,0.25), 0 10px 10px rgba(0,0,0,0.22);
            position: relative;
            overflow: hidden;
            width: 450px;
            max-width: 100%;
            min-height: 480px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 40px;
            text-align: center;
        }
        
        #resetPasswordForm {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        #resetPasswordForm img {
            margin-bottom: 20px;
            width: 130px;
        }

        #resetPasswordForm h2 {
            margin-bottom: 10px;
            color: #1a365d;
        }

        #resetPasswordForm p {
            font-size: 14px;
            color: #718096;
            line-height: 1.5;
            margin-bottom: 25px;
        }

        /* Input y Button heredan de Style.css, pero aseguramos ancho */
        input {
            background-color: #eee;
            border: none;
            padding: 12px 15px;
            margin: 8px 0;
            width: 100%;
            border-radius: 5px;
        }

        button {
            border-radius: 20px;
            border: 1px solid #185a9d;
            background-color: #185a9d;
            color: #FFFFFF;
            font-size: 12px;
            font-weight: bold;
            padding: 12px 45px;
            letter-spacing: 1px;
            text-transform: uppercase;
            transition: transform 80ms ease-in;
            cursor: pointer;
            margin-top: 15px;
            width: 100%;
        }

        button:active {
            transform: scale(0.95);
        }

        .back-link {
            color: #185a9d;
            font-size: 14px;
            text-decoration: none;
            margin-top: 30px;
            font-weight: 600;
        }

        .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <div class="container-reset" id="resetContainer">
        <form id="resetPasswordForm" action="/api/recuperar" method="POST">
          
            <img src=Logo.jpg alt="TESCHI LOGO">
            
            <h2>¿Olvidaste tu Contraseña?</h2>
            <p>
                Ingresa tu correo institucional registrado. Te enviaremos un código para restablecer tu acceso.
            </p>
               
            <input type="email" placeholder="ejemplo@teschi.edu.mx" name="email" required />
               
            <button type="submit">ENVIAR INSTRUCCIONES</button>
          
            <a href="/" class="back-link">← Volver a Iniciar Sesión</a>
        </form>
    </div>

    <script>
        // Opcional: Manejo de respuesta visual rápida
        const form = document.getElementById('resetPasswordForm');
        form.onsubmit = () => {
            // Podrías mostrar un spinner aquí
            console.log("Enviando solicitud de recuperación...");
        };
    </script>

</body>
</html>